# Codex: Start Here

このフォルダを開いたら、いきなり大規模リファクタをせず次の順で進めてください。

1. `AGENTS.md` を読む
2. `docs/CURRENT_STATE.md` を読む
3. `docs/JOYCON_NEXT_STEPS.md` を読む
4. `npm test` を実行
5. ユーザーのMac実機で `START_JOYCON_DRY_RUN.command` を確認
6. 実機差分だけを最小修正

現在の最優先は **Joy-Con 2版を実用品として動かすこと**。
M5Stack版の実装や過剰な共通化はまだ始めない。
