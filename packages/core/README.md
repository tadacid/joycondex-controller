# packages/core

将来、Joy-Con 2版とM5Stack版で共有する純粋な状態機械・意味イベントを置く予定です。

候補:

- Semantic Action definitions
- TALK/draft state
- ACTION/CANCEL context state
- Agent/Thread selection
- Attention Queue
- Safety policyのデバイス非依存部分

Joy-Con V0の実機検証前に、抽象化だけを目的として既存コードを移動しないでください。
